# 2026-05-27 工作日志

## L1 寄存器构建
- 创建 `elite_register.py`，实现 Elite 6层架构的 L1 寄存器层
- 6 功能槽位：TASK/CONTEXT/PENDING/TEMP/RECENT/SCRATCH，各有默认 TTL
- 支持 set/get/delete/list/expire/promote/snapshot/status/clear/session 命令
- JSON 持久化存储于 ~/.openclaw/memory/register/
- L1→L2 晋升通道验证通过：promote() 成功写入 LanceDB，语义搜索 dist=0.0721
- TTL 过期验证通过：2秒 TTL 的条目 3 秒后自动清除
- 生成一页纸可视化：L1寄存器架构_一页纸.html

## L3 冷存储构建
- 创建 `elite_coldstore.py`，实现 Elite 6层架构的 L3 冷存储层
- SQLite+FTS5（unicode61 tokenizer + trigger 自动同步），替代原设计中的 Git Notes
- 核心能力：archive(归档)、restore(恢复到L2)、search(FTS5+LIKE双通道)、aging(自动老化)、version_log、snapshot
- FTS5 中文搜索：拆单字OR搜索 + LIKE短语兜底，验证"AI培训师""飞书""寄存器"全部命中
- L3→L2 恢复验证通过：restore() 成功写入 LanceDB
- 修复 FTS5 content table 不同步问题 → 改用 trigger 自动同步
- 修复 FTS5 中文分词问题 → unicode61 + 单字OR + LIKE兜底
- 生成一页纸可视化：L3冷存储架构_一页纸.html

## L3 逆向同步（飞书→本地）
- 在 `elite_sync.py` 中新增 L3 冷存储逆向同步能力
- 新增 `pull --with-cold` 命令：飞书 → LanceDB → L3冷存储，一步到位
- 新增 `sync-cold` 命令：LanceDB 全部记录 → L3冷存储（新机器恢复后执行）
- `pull_from_feishu()` 增加 `to_cold` 参数，拉取时同步写入 L3
- 去重由 ColdStore 的 content_hash 天然保证，重复归档自动跳过
- 验证通过：sync-cold 归档 8 条（6 条写入，2 条去重跳过），pull --with-cold 正常输出

## L4 文件记忆构建
- 创建 `elite_filestore.py`，实现 Elite 6层架构的 L4 文件记忆层
- SQLite+FTS5 文件索引，存储于 ~/.openclaw/memory/filestore/
- 核心能力：index(目录/文件索引)、search(FTS5+LIKE双通道中文搜索)、list(按类型/标签筛选)、info(文件详情)、relate(文件关系)、watch(变更检测)、tag(标签管理)、aging(旧文件→L3归档)、promote(L4→L2向量化)、snapshot、status、log
- 自动跳过 .git/__pycache__/node_modules/venv 等目录
- 文件分类：code/document/config/data/image/media/log/other
- 二进制文件仅存元数据，文本文件索引内容+描述
- L4→L2 推送：文件内容向量化写入 LanceDB，支持语义搜索
- L4→L3 老化：90天未访问的文件记录归档到冷存储
- 验证通过：索引14个文件、搜索"register"命中、文件关系建立、标签添加、变更检测3个新增文件、L4→L2推送成功

## L4 飞书映射
- 在 `elite_filestore.py` 中新增 L4 <-> 飞书多维表格双向映射能力
- 复用同一个飞书Elite记忆库表（tblZxOCAmGAk84cJ），通过 记忆层级=L4-文件 区分
- 新增 CLI 命令：push（L4→飞书）、pull（飞书→L4）、sync-status（同步状态查看）
- push_to_feishu()：将文件元数据（名称/路径/类型/大小/行数/标签/描述）格式化为飞书记忆文本
- pull_from_feishu()：从飞书解析L4记录，写入本地文件索引（含 feishu_id 映射）
- _parse_feishu_text()：逆向解析飞书文本格式恢复文件信息
- 同步状态管理：filestore.json 的 feishu_sync 字段跟踪 synced_file_hashes/synced_feishu_ids
- 本地 files 表的 feishu_id 字段存储飞书记录 ID，用于增量同步判断
- 14/14 文件记录全部推送到飞书，feishu_id 双向映射已更新
- 飞书写入需 useUAT=true（用户访问令牌），tenant token 无权限

## L5 语义压缩构建
- 创建 `elite_semantic.py`，实现 Elite 6层架构的 L5 语义压缩层
- SQLite+FTS5 压缩索引，存储于 ~/.openclaw/memory/semantic/
- 压缩引擎：extractive（语句评分+关键词提取+实体识别）
  - 按位置/关键词密度/实体密度评分句子，保留 top-N 至目标压缩比
  - TF-based 关键词提取（中英文分词）
  - 正则实体识别（路径/ID/大写术语）
- 核心能力：add（压缩+存储）、search（FTS5+LIKE双通道中文搜索）、list、info、decompress（从源层恢复原文）、auto-compress（从L2/L3/L4自动压缩）、push/pull/sync-status（飞书映射）
- 验证：add 1条手动 + auto-compress 5条（来自L4），共6条，平均压缩比31.8%
- decompress 验证：成功从 L4 文件记忆恢复原文
- 飞书双向映射：6/6 记录推送到飞书 Elite记忆库（记忆层级=L5-压缩），feishu_id 双向映射已更新
- 飞书文本格式：压缩文本 + [L5-压缩] 标记 + 关键词/实体/压缩比/方法/原文层/原文ID/原文哈希/原文长度/压缩长度

## L6 知识图谱构建
- 创建 `elite_knowledge.py`，实现 Elite 6层架构的 L6 知识图谱层
- SQLite+FTS5 实体+关系索引，存储于 ~/.openclaw/memory/knowledge/
- 数据模型：entities 表（id/name/type/layer_source/source_id/description/attributes）+ relations 表（id/subject_id/predicate/object_id/confidence/evidence/layer_source）
- 核心能力：add-entity/add-relation/auto-extract/search/traverse/path/neighbors/list-entities/list-relations/info/merge/push/pull/sync-status/snapshot/status/log
- auto-extract 从 L2-L5 自动提取：层名实体(L1-L6)、路径实体、技术名词(驼峰/连字符)、中文概念关键词
- 图遍历：traverse（BFS 任意深度）、path（两实体间最短路径）、neighbors（直接邻居）
- 合并实体：merge() 将重复实体合并（关系重定向+描述合并）
- 飞书映射：记忆层级=L6-图谱，实体文本格式 [实体] name + [L6-图谱] + 类型/来源层/描述，关系文本格式 [关系] s--p-->o + 主语/谓词/宾语/置信度
- 验证：auto-extract 提取 45 个实体 + 11 个关系
  - 类型分布：concept(37) + layer(6) + technology(1) + tool(1)
  - 谓词分布：同步(4) + 归档(2) + 晋升(2) + 恢复(1) + 映射(1) + 关联(1)
  - 层间关系：L1-晋升->L2, L2-归档->L3, L3-恢复->L2, L4-晋升->L2, L4-归档->L3, L5-映射->L2, L6-关联->L5, L2/4/5/6-同步->飞书
- 图遍历验证：L1 出发 depth=3 覆盖 7 节点 11 边；L6→飞书最短路径 1 跳；L2 有 6 个连接
- 飞书核心同步：7 层级实体 + 11 关系 = 18 条核心记录推送完成，feishu_id 双向映射已更新
- 剩余 38 条概念实体待后续补充推送
