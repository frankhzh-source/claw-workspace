# 2026-06-17 工作日志

## 数据飞轮（Data Flywheel）搭建操作手册
- 基于纳德拉"Token资本+人力资本"框架 + 与用户的深入讨论，完成操作手册级精度的搭建文档
- 11章完整覆盖：系统总览 → 感知层 → 反馈层 → 记忆层(Elite) → SOP层 → 标准层 → 正反馈机制 → 衰减机制 → 数据库层 → 日常运营SOP → 故障处理手册
- 核心创新：负反馈管理机制（失败数据作为养料）、衰减机制（TTL+年龄衰减）、跨层搜索策略（L2→L6→L4→L3优先顺序）
- 产出：数据飞轮_搭建操作手册.md（原命名"爬山机器"，应要求改为更大众认可的"数据飞轮"）
- 同时讨论了纳德拉"爬山机器"概念与用户现有三层布局/Elite系统/LOOP方案的对应关系

## SVG修复 — 数据飞轮架构总览（15:34）
- **根因**：SVG中 `<marker>` 箭头使用了 `stroke="currentColor"`，该CSS关键字在飞书画板/部分SVG渲染器中解析失败，箭头变为不可见或黑块；另外第29行`<line>`同时存在`stroke="#378ADD"`和`stroke="currentColor"`，后者覆盖前者导致边框消失
- **修复**：3个arrow marker全部改为硬编码颜色(`#534AB7`/`#BA7517`/`#378ADD`)；删除重复`stroke`属性
- **教训**：生成SVG必须避开三个坑：(1) CSS变量 (2) currentColor (3) 重复属性；全部用硬编码hex值
- 产出：数据飞轮_系统架构总览.svg

## PRD文档检查（15:41）
- 读取了飞书 PRD 文档（达芬奇密码 AI 素材工作台 - 内部产品化一阶段完整 PRD V2）
- 完整文档含 12 个子文档：PRD主文、handoff、data-model、api-contract、model-channel-contract、prompt-library、algorithm-evaluation、deployment、development-roadmap、prd摘要、prototype、collaboration
- 检查发现5个问题：Mermaid图渲染丢失、缺图片合成服务选型风险、成本口径不统一、评测通过策略缺失、AI助手权限边界已有但缺人机确认异常处理
- 与用户项目关联度：模型通道适配器（与LoRA本地模型对接）、三层生图方案（与PRD任务类型映射）、模板/审核/复盘逻辑（与数据飞轮手册一致）

## LoRA本地模型对接PRD工作台 — 完整方案（15:41）
- 从全栈开发视角完整梳理了LoRA本地模型如何以「模型通道」形式接入AI素材工作台
- 明确了用户的角色边界：只负责第三层（LoRA训练+模型管理+推理服务），不接触前端/后端/队列
- 拆解了PRD四类任务与三层生图的映射关系：上身/三视图→L2，主图/场景图/细节图→L3
- 核心洞察：L3不需要单独训练LoRA，只需要L1+L2组合+换Prompt
- 详细描述了4个模块：训练流水线→模型仓库→推理HTTP服务→迭代优化循环
- 给出了从Day1到Day4的实操行动建议
- 产出：LoRA本地模型对接PRD工作台_完整方案.md
- 应要求生成了飞书文档：https://cp8z7brjmy.feishu.cn/docx/RyAfdQqzFoFArxxgfNbcUdtInYb

## 直播运营数据飞轮功能体系（17:20）
- 将数据飞轮六层架构落地到直播运营领域
- 感知层：千川/抖店API/评论区/竞品监控数据采集方案
- 反馈层：正反馈（高转化/爆款/高光时刻）和负反馈（掉人/低转化/评论区负面）分类体系
- 记忆层：Elite映射，每场直播结构化记忆+衰减策略
- SOP层：直播标准化流程（开播→引流→主推→憋单→收尾）
- 标准层：5个通用指标+品类专项+主播评分
- 三个核心场景：单场复盘 / 主播能力 / 品类选品
- 日常操作清单：每日/每周/每月操作规范
- 产出：直播运营数据飞轮_架构图.svg + 直播运营数据飞轮_功能体系手册.md

## 整合已有直播资产到飞轮手册 v2（17:29）
- 查找了 workspace_kiwi 下的5套直播 SKILL 和知识库资产
- 将六卖神剑话术框架嵌入 SOP 层，替换原通用 SOP
- 将抖音官方CORE方法论数据标准（5级评分制/品类基准/流量结构/异常诊断）嵌入标准层
- 将五维四率转化力分析嵌入反馈层
- 将主播培训师90天训练路径和46点体系嵌入正反馈循环
- 将 douyin-validator 的直播间健康度评分逻辑嵌入数据异常诊断
- 输出：直播运营数据飞轮_功能体系手册.md（v2整合版，含附录资产索引）

## SVG修复 + 术语修正（17:39）
- [问题1] SVG简化了"六卖神剑"为"六剑"，违反用户明确要求 → 全图替换，同时修正手册中所有"六剑"缩写（10处自动替换+2处手动修正），仅保留"第三剑至第六剑"等剑序数（非框架名缩写）
- [问题2] 文字溢出容器：原因→SVG `<text>` 基线定位未预留 descender（下行部分），底部3px被容器裁切；另右侧68px标签栏过窄导致screenshot等长文本紧贴边界
- [根因] SVG使用`text-anchor="middle"`时依赖手动计算「容器高度≥基线y+字号+2px」的规则，上次数值踩线未留安全边距
- [修复] ①所有容器高度+10px保留空间 ②去掉右侧68px标签栏（改为全宽600px主容器，信息已足够） ③底部3个资产卡片高度从56→78px，每行间留足18px间距

## 紧急修正：六卖神剑错误版本（17:45）
- 用户指正：six-swords.md中六卖神剑被错误写为"卖点挖掘→卖点包装→卖点信任→卖点成交→卖点裂变→卖点复购"
- 正确版本（来自六卖神剑话术体系.md）：品牌信任→款式匹配→渠道优势→紧迫感→互动→转粉
- 对应问题：为什么买这个品牌→为什么买这个款→为什么跟我买→为什么现在买→互动→转粉
- 修正范围：直播运营数据飞轮_功能体系手册.md（SOP层全章重写）、整合架构图SVG（第26行/37-38行）、4个six-swords.md副本全部覆盖
- 教训：six-swords.md是旧版内容，正确来源是obsidian-ai-knowledge下的六卖神剑话术体系.md

## 数据飞轮技术+商品化整合文档（18:00）
- 整合六层技术架构（感知→反馈→记忆→SOP→标准→数据库）+ 资产沉淀池（含合规三条款）+ 商品化三层路径（L1诊断/L2基准/L3飞轮）
- 生成完整SVG整合图 + 飞书文档（无Kiwi区块，仅作者信息）
- 飞书文档：https://cp8z7brjmy.feishu.cn/docx/GRfxdwVNbonV4ix3fx8cd1ksncg

## 抖音2026新规合规排查（18:08）
- 按2026最新抖音规则排查了six-swords.md、功能手册、飞书文档
- 核心新规：2026年4月17日生效的「限时限量噱头营销」规则 + 六大红线（广告法极限词/功效夸大/医疗敏感/违规引流/绝对承诺/限时限量）

## KIWI日记Day167生成+推送（18:30）
- 主题：600亿买一个工具，和一场直播的经验谁更值钱？
- 今日头条：DeepSeek 510亿融资/SpaceX收购Cursor/WAIC 2026官宣/硅基流动20亿B轮/OpenAI烧钱37亿/Qwen-Robot/AI眼镜大会
- Kiwi视角：将今天的"诊断经验资产化"讨论与百亿级AI融资新闻对照，核心观点——"诊断坐标系"比"AI工具"值钱
- 推送至公众号草稿箱成功（media_id返回）
- 文件：KIWI日记_Day167_2026-06-17.md / KIWI日记_Day167_2026-06-17_公众号.html
- 发现并修复的问题：①"顶级"标签改为"优秀级"（极限词风险）②紧迫感话术补充2026新规合规红线③six-swords.md第四剑补充新规禁止项④标准层新增6.6节「抖音2026合规话术标准」（六大红线+自查流程+定期排查）
- 同步更新了4个six-swords.md副本
