## 2026-07-16

### GitHub 备份测试
- 初始化 Claw 工作空间为 git 仓库（500个文件，~9.4MB）
- `.gitignore` 排除了 `workspace_kiwi/`、`__pycache__/`、`_cover_*.png` 等临时文件
- 11个文件中的飞书 App Secret 硬编码改为 `os.environ.get("FEISHU_APP_SECRET", "")`
- 仓库：https://github.com/frankhzh-source/claw-workspace
- 后续备份：`git add -A && git commit -m "backup $(date +%Y-%m-%d)" && git push`

### 跨电脑双向同步系统
- 创建 `sync_all.py`：双向同步脚本（push/pull/both 三种模式）
  - 用户级：workbuddy-sync 仓库 ←→ ~/.workbuddy/（MEMORY/SOUL/IDENTITY/USER/skills/mcp.json）
  - 项目级：claw-workspace 仓库 ←→ Claw 项目（含 .workbuddy/memory/ 项目记忆）
- 创建 `setup_sync.bat`：对面电脑一键初始化脚本
- 更新自动化：每小时执行 `sync_all.py both`（先 pull 再 push）
