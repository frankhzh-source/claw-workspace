# 信息防呆设计机制 v1

> **建立原因**：2026-08-29 至 08-30，两天内连续发生 **7 次信息错误**。其中 **5 次是用户追问发现的**。
> 如果当时没有追问，这些错误会直接流进商业决策。
> **结论：靠注意力不可靠，必须靠机制。**
> **作者**：海风 · AI 落地咨询 & GEO 优化
> **微信**：frankhzheng
> **版本**：v1（2026-08-30）

---

## 一、为什么"注意"没用

### 1.1 数据说话

| 指标 | 数值 |
|---|---|
| 两天内错误次数 | **7 次** |
| 其中用户追问发现 | **5 次（71%）** |
| 我自己发现 | 2 次（工具辅助） |
| 每次犯错后是否立了规矩 | **是，立了 5 次，仍然继续犯** |

### 1.2 三个根因

**根因一：缺失"信息分级"机制**
要么全信（二手源污染 5 次），要么全疑（判断摇摆 1 次）。**没有按证据强度分级**。

**根因二：GEO 领域的递归污染**
> **我们研究 GEO 污染，但获取信息的渠道（AI 检索）本身就被 GEO 污染。**
>
> 我检索"GEO 效果验证"，AI 给我推软文——**软文排前面，恰恰因为它 GEO 做得好**。

**根因三：规矩在记忆里，不在流程里**
两天立了 5 次规矩仍继续犯。**靠自觉不可靠，靠流程才可靠。**

---

## 二、防呆十原则（poka-yoke）映射到内容生产

源自丰田生产方式的防呆法（poka-yoke），改编用于内容生产：

| # | 原则 | 内容生产的应用 | 状态 |
|---|---|---|---|
| 1 | **断根** | 引用只能从「已核实信息库」取，不临时检索 | ✅ 信息库 |
| 2 | **自动** | 核验不通过 → **拒绝生成 PDF**（异常即停） | ✅ deliver.py |
| 3 | **顺序** | verify → md2print → PDF，跳过即失败 | ✅ deliver.py |
| 4 | **层别** | L0–L3 四级标注，显性区分可信度 | ✅ 规范 |
| 5 | **隔离** | 已核实 / 待核实 / 已否证 三区分离 | ✅ 信息库 |
| 6 | **相符** | 引用条款必须「页码 + 50 字符上下文」匹配 | ✅ 规范 |
| 7 | **警告** | 🚨必改 / ⚠️待核 / ✅通过 三级输出 | ✅ _verify.py |
| 8 | **复制** | 新文档套用模板（含强制核验章节） | ⏳ 待建 |
| 9 | **保险** | 关键数字需双 L1 源交叉（单一来源自动降级） | ⏳ 待建 |
| 10 | **缓和** | 错误流出后 72h 内更正并通知 | ⏳ 流程待定 |

---

## 三、已落地的五层防呆机制

### 层 1 · 分级制度（断根 + 层别）

| 等级 | 定义 | 使用规则 |
|---|---|---|
| **L0 一手源** | 原文 PDF / 证书 / 官方公告 / 官网 / 工商 | **可直接引用** |
| **L1 权威二手** | 央媒报道 / 官方名单 | 可引用，标来源 |
| **L2 一般二手** | 百科 / 技术社区 / 行业媒体 | 必须标"待核实" |
| **L3 高危** | 服务商盘点 / 测评 / 排行榜 / 带数据的"实测" | **一律不引用**，只当反查线索 |

### 层 2 · 分类核验标准（相符）

| 断言类型 | 核验要求 |
|---|---|
| **数字** | ① 谁测的 ② 怎么测的 ③ 有无利益关系 |
| **标准条款** | 打开原文 PDF，给出**物理页码 + 50 字符上下文** |
| **公司名** | **三重验证**：工商 + 官网 + 独立信源 |
| **链接** | 状态码 + 标题 + 关键词 + PDF 文件头（四重） |

### 层 3 · 信息库（断根 + 隔离）

`信息核验规范与已核实信息库_v1.md` 含三个区：
- **已核实**：标准条款（到页码）、企业（三重验证结果）
- **已否证（黑名单）**：7 条不可再引用的信息
- **待核实**：不能当结论用

### 层 4 · 核验脚本（警告）

`_verify.py` 扫描 MD，输出：
- 🚨 **必改**：已否证信息被当事实引用（**阻塞**）
- 📝 **可保留**：更正说明中引用（正常）
- ⚠️ **待核**：数字 / 条款 / 公司名 / 链接（提醒）

### 层 5 · 交付流水线（自动 + 顺序）★核心

`deliver.py` 把核验变成**强制卡点**：

```
写 MD → 【强制卡点】verify → 不通过则拒绝出 PDF → 通过后转 PDF → 交付
```

> **这是防呆的精髓：不是"提醒你做"，而是"不做就不让你往下走"。**

---

## 四、工具使用手册

### 4.1 核验单份文档

```bash
python _verify.py "文档.md"
```

### 4.2 交付（核验 + 生成 PDF）

```bash
python deliver.py "文档.md"                 # 标题用文件名
python deliver.py "文档.md" "自定义标题"      # 指定标题
python deliver.py "文档.md" --check          # 只核验，不出 PDF
python deliver.py "文档.md" --force          # 强制出（会打印红色警告，慎用）
```

**退出码**：`0` 通过 / `1` 被卡点拒绝 / `2` 参数错误

### 4.3 全库回归扫描

```bash
python deliver.py --batch
```

> **建议每周跑一次**——防止新写内容引入已知错误。

---

## 五、漏洞发现与修复记录（重要）

防呆机制本身也会出错。**必须双向验证**：既能拦住错误，又不误杀正常内容。

### 5.1 测试方法论

| 测试 | 方法 | 期望 |
|---|---|---|
| **正向测试** | 造一份含黑名单错误的文档 | 被拦住，退出码 1 |
| **反向测试** | 全库 11 份正常文档 | 全部通过，无误杀 |

### 5.2 已修复的漏洞（3 个）

**漏洞 1：黑名单 + 白名单同行 → 误判为对比说明**

```markdown
鉴真数检和信通院都认为 GEO 很重要。
```

因为同行出现"信通院"（白名单），被豁免 → **漏网**。

> **修复**：收紧为三选一豁免——
> ① 有明确否证标记（❌/改为/自报…）
> ② 表格行 + 同行有白名单
> ③ 非表格行 + 白名单 + **转折词**（但/然而/实际/实为/笔误…）
> 其余一律视为误用。**原则：宁可多报，不可漏报。**

**漏洞 2：修正后误杀正常内容**

收紧规则后，下面这句被误判：

```markdown
中国新闻技联的发布公告正文写的是「T/CAPT 026—2024」，
但标准封面和页眉全部为「T/CAPT 026—2026」
```

它是**更正说明**，但因不是表格行被判为误用。

> **修复**：新增第 ③ 条豁免——非表格行 + 白名单 + 转折词 → 对比说明。

**漏洞 3：脚本刚建成就抓出 2 个我漏掉的真实错误**

| 错误 | 说明 |
|---|---|
| **三区分治章节号 4.3（实为 7.1）** | 我改了一下午文档都没发现 |
| ❌ **v2 第 4 步 SOP 仍写「推荐甲方用鉴真数检」** | 我改了 §5/§7，漏了第 4 步 |

> **这直接证明了工具化的价值：人肉检查必漏，脚本不会。**

---

## 六、防呆的边界（必须诚实说明）

### 6.1 脚本能拦住什么

✅ **已知错误**——黑名单里已记录的 7 条
✅ **结构性问题**——数字/条款/公司名/链接的提取与提醒

### 6.2 脚本拦不住什么

❌ **未知错误**——黑名单里没有的错误信息
❌ **逻辑错误**——推理错了但事实没错
❌ **遗漏错误**——该写的没写（脚本只看已写内容）
❌ **判断错误**——事实都对，但结论错了

### 6.3 因此，三条铁律不能丢

1. **黑名单必须持续维护**——每次发现新错误，立即加入 `BLACKLIST`
2. **人工判断不可替代**——脚本是筛子，不是大脑
3. **追问机制保留**——用户对关键结论的追问，仍是最后一道防线

> **一句话**：防呆机制把"失误率"从 71% 降下来，但**降不到 0**。**最后一道防线永远是人的判断。**

---

## 七、待完善的防呆点

| # | 防呆点 | 优先级 | 说明 |
|---|---|---|---|
| 1 | **文档模板**（复制原则） | 高 | 新文档强制含"数据来源分级"章节 |
| 2 | **双源交叉自动降级**（保险原则） | 高 | 关键数字单一来源时自动标"待核实" |
| 3 | **错误流出补救流程**（缓和原则） | 中 | 发现已交付文档有错，72h 内更正并通知 |
| 4 | **定期回归自动化** | 中 | 每周自动跑 `--batch` 并报告 |
| 5 | **信息库版本化** | 低 | 每次更新信息库留痕，可追溯 |

---

## 八、一句话总结

> **检索 ≠ 核实。**
> **提醒 ≠ 防呆。**
> 在 GEO 领域尤其如此——**你用来检索的工具，本身就是被 GEO 优化的对象。**

防呆的本质不是让自己更小心，而是**让错误没有机会发生，或者一发生就被拦下**。

---

## 附：脚本位置

| 文件 | 用途 | GitHub |
|---|---|---|
| `_verify.py` | 信息核验工具 | https://github.com/frankhzh-source/claw-workspace/blob/master/_verify.py |
| `deliver.py` | 交付流水线（防呆卡点） | https://github.com/frankhzh-source/claw-workspace/blob/master/deliver.py |
| `信息核验规范与已核实信息库_v1.md` | 制度 + 资产库 | 本地 |

---

**作者信息**

**海风** · AI 落地咨询 & GEO 优化

微信：frankhzheng（加微信请备注「GEO」来源）

开源：https://github.com/frankhzh-source/claw-workspace
